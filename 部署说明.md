# 个人博客网站 Docker 容器化部署说明

## 一、部署架构

```
用户 --> Cloudflare (HTTPS/CDN) --> 服务器 Nginx(:28080)
                                       |
                                       ├── 前端静态文件 (/)
                                       └── 反向代理 (/api/v1) --> Node.js 后端 (:3000)
                                                                       |
                                                                       └── MySQL (:3307)
```

三个 Docker 容器通过 docker-compose 编排：
- **Nginx**：托管前端静态文件 + 反向代理后端 API
- **Backend**：Node.js/Express 后端服务
- **MySQL**：数据库服务

Cloudflare 处理 HTTPS 终止和 CDN 缓存，服务器默认监听 `28080` 端口。若该端口冲突，可修改 `.env.production` 中的 `PUBLIC_HTTP_PORT`。

## 二、服务器信息

| 项目 | 值 |
|------|-----|
| 服务器 IP | `<your-server-ip>` |
| 域名 | `<your-domain>` |
| SSH 用户 | root |
| 部署目录 | /opt/blog |
| Nginx 端口 | 28080（宿主机，默认）→ 80（容器） |
| MySQL 端口 | 3307（宿主机）→ 3306（容器） |
| Backend 端口 | 3000（容器内部） |

## 三、本次创建/修改的文件清单

### 新增文件

| 文件路径 | 用途 |
|---------|------|
| `blog-backend/Dockerfile` | 后端多阶段 Docker 构建文件（编译 TS → 生产运行） |
| `blog-backend/.dockerignore` | 后端 Docker 构建排除规则 |
| `blog-frontend/Dockerfile` | 前端多阶段 Docker 构建文件（Vite 编译 → Nginx 托管） |
| `blog-frontend/.dockerignore` | 前端 Docker 构建排除规则 |
| `nginx/default.conf` | Nginx 配置：静态文件服务 + API 反向代理 + gzip 压缩 |
| `docker-compose.yml` | Docker Compose 编排文件，定义 Nginx、Backend、MySQL、Redis、Qdrant 与迁移服务 |
| `.env.production` | 生产环境变量（数据库密码、JWT 密钥、CORS 域名等） |
| `.env.production.example` | 生产环境变量模板，提交代码时使用模板，不提交真实密钥 |
| `database/migrations` | 可重复执行的数据库迁移脚本，现有数据卷升级时自动补表 |
| `deploy.ps1` | 本地 Windows PowerShell 一键部署脚本 |
| `deploy-server.sh` | 服务器端自动化部署脚本（安装 Docker、生成密钥、启动容器） |
| `部署说明.md` | 本文档 |

### 修改的文件

| 文件路径 | 修改内容 |
|---------|---------|
| `blog-backend/src/app.ts` | CORS 配置改为通过 `CORS_ORIGIN` 环境变量动态配置域名白名单 |
| `database/migrations` | 增加运行期表迁移，修复旧数据卷缺表导致接口 500 的问题 |

## 四、端口映射说明

由于服务器上已有服务占用默认端口（80、3306），采用以下映射避免冲突：

| 服务 | 容器端口 | 宿主机端口 | 说明 |
|------|---------|-----------|------|
| Nginx | 80 | **`${PUBLIC_HTTP_PORT:-28080}`** | 避免与服务器已有 Web 服务冲突 |
| MySQL | 3306 | 无映射 | 仅 Docker 内部网络访问 |
| Redis | 6379 | 无映射 | 仅 Docker 内部网络访问 |
| Qdrant | 6333/6334 | 无映射 | 仅 Backend 内部访问，不暴露公网 |
| Backend | 3000 | 无映射 | 仅通过 Docker 内部网络被 Nginx 反代访问 |

## 五、Cloudflare 配置要点

1. **DNS 设置**：A 记录 `<your-domain>` → `<your-server-ip>`（开启橙色云朵代理）
2. **SSL/TLS**：加密模式选 **Flexible**（Cloudflare 到源站走 HTTP）
3. **Origin Rules**：由于服务默认监听 `28080` 端口，需添加规则：
   - 路径：Rules → Origin Rules → 创建规则
   - 条件：主机名等于 `<your-domain>`
   - 操作：Destination Port 覆盖为 `.env.production` 中的 `PUBLIC_HTTP_PORT`，默认 `28080`
4. **推荐开启**：Always Use HTTPS、Brotli 压缩、Auto Minify

## 六、常用运维命令

SSH 登录服务器后，在 `/opt/blog` 目录下执行：

```bash
# 查看容器状态
docker compose --env-file .env.production ps

# 查看实时日志
docker compose --env-file .env.production logs -f

# 仅查看某个服务日志
docker compose --env-file .env.production logs -f backend
docker compose --env-file .env.production logs -f nginx
docker compose --env-file .env.production logs -f mysql
docker compose --env-file .env.production logs -f migrate

# 重启所有服务
docker compose --env-file .env.production restart

# 重新构建并启动（代码更新后）
docker compose --env-file .env.production up -d --build

# 停止所有服务
docker compose --env-file .env.production down

# 停止并删除数据卷（清除数据库数据，慎用！）
docker compose --env-file .env.production down -v

# 查看数据库密码（部署时自动生成的）
cat .env.production | grep DB_PASSWORD

# 进入 MySQL 容器
docker compose --env-file .env.production exec mysql mysql -u root -p my_blog

# 备份数据库
docker compose --env-file .env.production exec mysql mysqldump -u root -p my_blog > backup.sql
```

## 七、更新部署流程

代码修改后，重新部署步骤：

### 方式一：使用一键部署脚本（本地执行）
```powershell
cd E:\个人博客网站
.\deploy.ps1
```

### 方式二：手动更新
1. 本地打包上传：
```powershell
cd E:\个人博客网站
tar -czf blog-deploy.tar.gz --exclude="node_modules" --exclude=".git" --exclude="blog-frontend/dist" --exclude="blog-backend/dist" blog-backend blog-frontend database nginx docker-compose.yml .env.production deploy-server.sh
scp blog-deploy.tar.gz root@<your-server-ip>:/opt/blog/
```

2. 服务器上重新构建：
```bash
cd /opt/blog
tar -xzf blog-deploy.tar.gz
docker compose --env-file .env.production up -d --build
```

## 八、部署过程中解决的问题

| 问题 | 解决方案 |
|------|---------|
| 前端 `package-lock.json` 不同步 | 改用 `npm install` 代替 `npm ci` |
| 前端 `vue-tsc` 类型检查报错 | 跳过类型检查，直接 `vite build` |
| 端口 8080 被 PM2 旧应用占用 | 停止并删除 PM2 中的 `api-backend` 进程 |
| 端口与其他代理服务冲突 | 修改 `.env.production` 中的 `PUBLIC_HTTP_PORT`，例如 `28080`、`28081` |
| `.env.production` 密钥未配置 | 服务器上自动生成安全随机密钥 |
| 旧数据库卷缺少新增表 | `migrate` 服务自动执行 `database/migrations/*.sql` 补齐结构 |

## 九、安全注意事项

1. **SSH 密码**：建议部署完成后立即更改（`passwd root`）
2. **数据库与内部服务**：MySQL、Redis、Qdrant 不映射宿主机端口，不暴露到公网
3. **JWT 密钥**：已自动使用 `openssl rand -hex 32` 生成强密钥
4. **防火墙**：服务器仅开放 `PUBLIC_HTTP_PORT` 对应端口和 22 端口
5. **`.env.production`**：包含敏感信息，不要提交到 Git 仓库；提交模板 `.env.production.example`
